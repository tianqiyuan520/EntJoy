# 2026-08-28 查询缓存共享与 N 元组查询生成器

> 本次改动覆盖：EntityQuery 共享注册表（S20 查询缓存部分）、Entity Group 反向索引、
> N 元组查询 SourceGenerator（`world.Query<T0..Tn>()`）、SystemAPI 移除、09_ECS 目录整理。

## 一、背景

原 `World.CreateEntityQuery` 每次调用都 `new EntityQuery` 并全量扫描所有 Archetype（`EntityQuery.cs` 构造即 `Refresh()`），
相同规则的查询被重复构造 + 重复扫描。`world.Query<T0, T1>()` 固定 2 组件上限，3+ 组件查询编译不过。

## 二、EntityQuery 共享注册表（S20 查询缓存）

### 设计

| 组件 | 说明 |
|---|---|
| `QueryKey` | 规则指纹：All/Any/None/AllEnabled 的 ComponentType.Id 序列 + SharedFilter。**排序归一**（`Array.Sort`）——匹配语义与顺序无关，`WithAll<A,B>` 与 `WithAll<B,A>` 共享同一实例 |
| `World.GetOrCreateEntityQuery` | 按 `QueryKey` 查表共享实例，O(1) 命中，避免重复构造 + 全量重扫 |
| `EntityQuery.RefreshIncremental` | 两级判定：Archetype 集合未变化（`archCount == _scannedArchetypeCount`）→ 复用签名匹配集合，只重收 chunk；Archetype 增删 → 全量重扫 |
| 惰性刷新 | 访问时版本检查（`StructuralVersion` 对比），结构变更路径零额外成本，无需 EntityManager→World 通知 |

### 基准（09_ECS/Benchmarks/EntityQueryCacheBenchmark.cs）

5 万实体 / 4 Archetype / 200 迭代：

| 指标 | 结果 |
|---|---|
| 共享查表 vs 重复构造 | 0.0009 ms vs 0.0035 ms = **4.12x 提速** |
| 增量刷新正确性 | delta 10000 = 预期 10000 OK |
| 增量刷新耗时 | 0.0454 ms/iter（100 新实体 + 查询） |

## 三、Entity Group 反向索引

### 设计

- **结构**：`Archetype → List<EntityQuery>`（Archetype 匹配哪些查询）
- **查询路径**：`world.GetGroupsOf(entity)` = 实体定位 Archetype（O(1)，复用 `EntityIndexInWorld`）+ Archetype→查询查表
- **惰性构建**：`_groupIndexVersion` 聚合版本（查询数 + 各查询 StructuralVersion 之和）判脏，变化才重建——结构变更路径零额外成本
- **API**：返回 `IReadOnlyList<EntityQuery>`（只读视图，零拷贝）

### 实测（基准第 3 部分）

| 验证点 | 结果 |
|---|---|
| 实体 #0（[P,V]）匹配 | qPV + qP，不含 qH ✅ |
| AddComponent\<Health\> 后 | 含 qH ✅ |
| RemoveComponent\<Velocity\> 后 | 不含 qPV ✅ |
| GetGroupsOf 性能 | **0.08 us/iter** |

## 四、N 元组查询生成器（QueryTupleSourceGenerator）

### 需求

`world.Query<Position, Velocity, Health>()`（3+ 组件）原编译不过——`QueryEnumerable<T0,T1>` 固定 2 上限。

### 实现（A 方案：生成器生成完整枚举器）

| 步骤 | 说明 |
|---|---|
| `TempBuffer` public 化 | 生成器枚举器位图缓冲（用户程序集访问） |
| `QueryTupleSourceGenerator` | 扫描 `world.Query<T0..Tn>()` 调用点（N≥3），**按 arity 生成泛型模板**（`QueryEnumerable`/`QueryEnumerator`/`EntityQueryResult` N 元组版 + `QueryTupleWorldExtensions{n}` 扩展方法，body 内 `typeof(T0..Tn)` 泛型化） |
| `ECSSourceGenerator` 注册 | 复合生成器登记新子生成器 |

> **v2 重构（2026-08-29）：按 arity 而非按具体组合生成。**
> 原实现按"唯一元组组合"去重，同一 arity 不同组件组合会重复生成同名泛型类型定义（CS0101）；
> 扩展类固定名 `QueryTupleWorldExtensions` 在多个 arity 并存时同样冲突。
> 改为：只收集 arity（`SortedSet<int>`），每个 arity 产出一份泛型模板，
> 扩展方法签名 `Query<T0,T1,T2>(this World world)` 与 body 均泛型化，任何组件组合共用同一份定义，运行时零反射。

### 关键坑（调试记录）

1. **增量生成器鸡生蛋**：首次编译时 N 元组重载尚不存在，语义模型 `GetSymbolInfo` 找不到方法符号 → 不能依赖方法解析，改为语法级提取泛型实参 + `GetTypeInfo` 解析类型实参
2. **partial 跨程序集不合并**：生成器在用户程序集无法给库的 `World` 注入成员 → 用**扩展方法**（`this World world`）
3. **N≥3 限定**：2 元组库内已有 `QueryEnumerable<T0,T1>`，生成会重复定义冲突
4. **`global::` 前缀**：`ToDisplayString(FullyQualifiedFormat)` 输出 `global::EntJoy.ECS.World`，符号匹配需归一前缀
5. **SystemAPI 不能做扩展接收者**：静态类不能用作 `this` 参数 → 后随 SystemAPI 移除一并解决

### Config 收编（防硬编码/改名）

`Config.cs` 新增：`QueryMethod`/`WorldFullName`/`WorldTypeName`/`MinTupleArity`。
`GetTuples` 用符号匹配：`GetTypeInfo(ma.Expression)` 解析接收者类型 → 归一 `global::` → 必须等于 `Config.WorldFullName` 才生成。
变量名随意（world/w/myWorld）均可识别；改名/误用不误生成。

### 使用效果

```csharp
foreach (var r in world.Query<Position, Velocity, Health>())
{
    r.Comp0.X += r.Comp1.X * 0.016f;
    r.Comp2.Current -= 0.1f;  // Comp0/1/2 强类型
}
```

## 五、SystemAPI 移除

| 改动 | 说明 |
|---|---|
| 删除 `System/SystemAPI.cs` | `Query`/`QueryChunks` 原基于 `World.DefaultWorld` |
| `World` 新增 `QueryChunks<T0,T1>()` | 等价原 `SystemAPI.QueryChunks`（Chunk 级遍历） |
| 迁移 `ScheduleGraphDemo` | 3 处 `SystemAPI.Query` → `World.DefaultWorld.Query` |
| 迁移 `SpritesRandomMove`（Godot） | 2 处 `SystemAPI.QueryChunks` → `myWorld.QueryChunks` |

## 六、09_ECS 目录整理

```
09_ECS/
  ├─ Program.cs / Components.cs        （基础设施）
  ├─ Benchmarks/                        EnabledComparisonBenchmark / IJobEntityEnabledBenchmark
  │                                     ScheduleOverheadBenchmark / EntityQueryCacheBenchmark
  ├─ Demos/                             ObserverDemo / EventChannelDemo / ChangeTrackingDemo
  │                                     EntityBuilderDemo / ScheduleGraphDemo / SharedComponentDemo
  │                                     NativeJobSmokeTest / QueryTupleDemo（新增）
  └─ Jobs/                              EventChannelJobTest / NativeEventJobTest / ISpcEventJobTest
```

命名空间不变（与路径无关），csproj SDK 自动包含子目录，零代码改动。

## 七、验证

- 测试：EntityQueryCacheTests(8) + EntityGroupIndexTests(6) + QueryTupleTests(2) = 16 新增，全量 59/59 通过
- 样例：EntJoySample 0 错误，QueryTupleDemo 运行 OK（matched 5, sumX=100）
- Godot 样例：0 错误

## 八、遗留与后续

> **2026-08-29 更新**：1、2 已完成（见下"N 元组扩展（v2）"），3 确认保持单泛型（多 enable 过滤走 QueryBuilder 链式）。

1. ~~**`QueryBuilder.WithAll` 仅到 2 泛型**~~ → 已完成：`QueryBuilderTupleSourceGenerator` 按 arity 生成 `WithAll<T0..Tn>()` 扩展方法 + `internal ComponentTypes<T0..Tn>` 静态缓存
2. ~~**`World.QueryChunks` 仅 2 泛型**~~ → 已完成：`ChunkQueryTupleSourceGenerator` 生成 chunk 三件套（`ChunkEnumerable`/`ChunkEnumerator`/`ChunkResult` 含 `GetPtr0..N`/`GetSpan0..N`）+ `World.QueryChunks` 扩展方法
3. **`QuerySelection.WithEnabled` 2 上限**：低频便利链，保持单泛型 `WithEnabled<T1>`；多 enable 过滤用 `new QueryBuilder().WithAll<T>().WithEnabled<A>().WithEnabled<B>()` 链式（`With*` 均返回 `QueryBuilder` 可任意串联）
4. **SystemAPI N 元组**：随移除不再需要
5. `QueryTupleTests` 可补 N=4 用例验证更大 N 健壮性

### N 元组扩展（v2，2026-08-29）

新增两个生成器，均由 `ECSSourceGenerator` 登记：

| 生成器 | 产出 | 触发扫描 |
|---|---|---|
| `QueryBuilderTupleSourceGenerator` | `ComponentTypes<T0..Tn>` 内部静态缓存 + `QueryBuilderWithAllExtensions{n}.WithAll<T0..Tn>(this QueryBuilder)` | `builder.WithAll<A,B,C>()`（接收者=QueryBuilder，arity≥3） |
| `ChunkQueryTupleSourceGenerator` | `ChunkEnumerable/Enumerator/Result<T0..Tn>` + `ChunkQueryTupleWorldExtensions{n}.QueryChunks<T0..Tn>(this World)` | `world.QueryChunks<A,B,C>()`（接收者=World，arity≥3） |

设计要点：

- **扩展方法而非 partial**：生成代码编译进调用方程序集（generator 以 Analyzer 引用），跨程序集 partial 不合并 → 全部用 `this` 扩展方法
- **WithAll 内联 Merge**：扩展方法无法访问 `QueryBuilder.Merge`（private），类内自带等价 Merge（`QueryBuilderWithAllExtensions{n}.Merge`）；首调零分配（直接引用 `ComponentTypes<T0..Tn>.Share`）
- **QueryChunks 自包含**：扩展方法 body 直接构建 `builder.All`（`typeof(T0..Tn)` 数组），不依赖 WithAll 生成器——两生成器扫描面不同，用户可能只调 QueryChunks 不调 WithAll
- **`ChunkResult` 单泛型方法保留**：`GetEnabledMask<T>`/`IsComponentEnabled<T>`/`SetComponentEnabled<T>` 是单泛型通用方法，生成器不展开，原样保留
- **`BitMask` 构造器 internal→public**：生成代码编译进调用方程序集，`ChunkResult.GetEnabledMask` 需从 chunk 位图指针构造 BitMask
- **连带修复**：`BitMask` 构造器放宽访问（见上）；`QueryTupleWorldExtensions` 固定类名冲突在重构中一并解决（arity 后缀命名）

使用效果：

```csharp
// WithAll N 元组（一次调用替代三连链式）
var query = world.GetOrCreateEntityQuery(new QueryBuilder().WithAll<Position, Velocity, Health>());

// QueryChunks N 元组
foreach (var chunk in world.QueryChunks<Position, Velocity, Health>())
{
    var pos = chunk.GetSpan0();   // Span<Position>
    var vel = chunk.GetSpan1();
    var hp  = chunk.GetSpan2();
    for (int i = 0; i < pos.Length; i++) pos[i].X += vel[i].X;
}
```

验证：QueryTupleDemo 增加 QueryChunks 三组件遍历 + `WithAll<Position, Velocity, Health>` 单次调用，三断言均 OK（matched 5, sumX=100）；生成器/sample 0 错误。
